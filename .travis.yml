dist: trusty
sudo: required
language: cpp
compiler: gcc

os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - xorg-dev

      # SFML Requires
      - libudev-dev
      - libopenal-dev
      - libflac-dev
      - libvorbis-dev
      - libxrandr-dev

      - libxinerama-dev
      - libxcursor-dev
      - libxi-dev

before_script:
- mkdir -p build
- cd build
- export CC=gcc-7
- export CXX=g++-7
- cmake --version
- cmake ..
# - cmake -G "Visual Studio 15 2017 Win64"
# - cmake --build . --target

# - cmake --build . --target

# script:
  # - cmake --build
